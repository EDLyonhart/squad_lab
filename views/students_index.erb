<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Students Index</title>
</head>
<body>
  <h1>Current Students</h1>

  <% @students.each do |student| %>
    <h2><%= student["name"] %></h2>
     <p>
       <a href="/squads/<%= @squad["squads_id"] %>/students/<%= student["id"] %>">Show Student Info</a>
     </p>
     <p>
      <a href="/squads/<%= @squad["squads_id"] %>/students/<%= student["id"] %>/edit">Edit Student</a>
     </p>

     ------
  <% end %>

  <p>
    <a href="/squads/<%= @squad["squads_id"] %>/students/new">Add a New Student</a>
</body>
</html>




